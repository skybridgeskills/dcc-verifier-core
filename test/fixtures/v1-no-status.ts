/**
 * VC 1.1, Ed25519Signature2020, did:key issuer, no credential status.
 * Golden interoperability input (real signature).
 */
export { v1NoStatus } from '../../src/test-fixtures/verifiableCredentials/v1/v1NoStatus.js';
